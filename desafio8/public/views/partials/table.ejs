<div class="entry">
 
    <table class="table table-striped table-hover">
        <thead>
            <tr>
                <th scope="col">#</th>
                <th scope="col">Name</th>
                <th scope="col">Price</th>
                <th scope="col">Imagen</th>
            </tr>
        </thead>
        <tbody id="trTable">
            <% productsList.forEach(prod => { %>
                <tr>
                    <td> <%=prod.id %> </td>                
                    <td> <%=prod.name %> </td>
                    <td> <%=prod.price %> </td>
                    <td><img src="<%=prod.url %>" alt="<%=prod.name %>" width="25"></td>
                </tr>
            <% }) %>
        </tbody>
</table>
</div>